package oopang.view;

/**
 * Identifies a scene that can be loaded.
 */
public enum GameScene {
    MAIN_MENU,
    SELECT_PLAYERS,
    SELECT_MODE,
    SELECT_LEVEL,
    GAME_GUI,
    GAMEOVER,
    LEVEL_STEP,
    LEVEL_RESET,
    PAUSE;
}
